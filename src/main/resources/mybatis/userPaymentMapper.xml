<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="se.kth.iv1351.dao.UserPaymentMapper">
    <select id="selectUserPayment" parameterType="java.lang.Integer" resultType="se.kth.iv1351.model.UserPaymentData">
        SELECT * FROM USER_PAYMENTS WHERE id= #{paymentid};
    </select>
    <insert id="insertUserPayment" parameterType="se.kth.iv1351.openapi.model.UserPayment">
        INSERT INTO BOOKINGS (id, type, amount, currency, user_id)
        VALUES (#{id}, #{type}, #{amount}, #{currency}, #{userid});
    </insert>
</mapper>